import { XAmountOfCharactersPipe } from './x-amount-of-characters.pipe';

describe('XAmountOfCharactersPipe', () => {
  it('create an instance', () => {
    const pipe = new XAmountOfCharactersPipe();
    expect(pipe).toBeTruthy();
  });
});
